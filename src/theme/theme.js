// src/theme/theme.js
import { DefaultTheme } from 'react-native-paper';
export const theme = {
  ...DefaultTheme,
  colors: {
    ...DefaultTheme.colors,
    primary: '#ff6b6b', // bright coral
    accent: '#ffd166',
    background: '#fff8f0',
    surface: '#ffffff',
  },
};
